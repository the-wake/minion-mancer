import React from 'react';


function CardLayout() {

  return (
    <div id="card-area" className="grid grid-cols-4 gap-4 justify-center content-start">
      {/* Add prop for cards from local storage(?) and populate here. */}
    </div>
  );
};

export default CardLayout;
